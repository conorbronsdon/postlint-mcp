## Summary

<!-- What does this change and why? -->

## Checklist

- [ ] `npm test` passes (no network calls — this server makes none)
- [ ] Any limit added or changed cites a published source in `src/limits.ts`
- [ ] A new counting rule has a test that fails without it
- [ ] Tool responses stayed small (agents pay tokens per response)
- [ ] README tool table and platform table updated if either changed
